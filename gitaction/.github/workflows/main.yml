name: Test

on: [push]

jobs:
Test_job:
runs-on: ubuntu-latest
steps: 
id: 'hello'
 - uses: githubcustomaction/gitaction@v1
 with: 
     username: 'Praveen'
     run: echo " The time is ${{ steps.hello.outputs.time }}"
